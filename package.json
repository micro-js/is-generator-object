{
  "name": "@micro-js/is-generator-object",
  "description": "Check if generator.",
  "repository": "git://github.com/micro-js/is-generator-object.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2",
    "standard": "^5.4.1"
  }
}
