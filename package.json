{
  "name": "@f/is-generator-object",
  "description": "Check if generator object.",
  "repository": "git://github.com/micro-js/is-generator-object.git",
  "version": "1.3.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/is-iterable": "^1.0.1",
    "@f/is-iterator": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2",
    "standard": "^5.4.1"
  }
}
